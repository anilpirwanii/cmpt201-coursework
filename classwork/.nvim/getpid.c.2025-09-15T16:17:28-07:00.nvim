#include <stdio.h>
#include <sys/types.h>
#include <unistd.h>

int main() {
  pid_t getpid(void);
  pid_t getppid(void);

  pid_t pid = fork();

  if (pid > 0) {
    printf("Parent", pid);
  } else {
    printf("child");
  }
}
